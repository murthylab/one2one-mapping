Model weights for the knockout (KO), dropout (DO), and no-knockout-trained (noKO) networks.

These models were trained on ~450 experimental sessions of natural courtship, including LC-silenced male flies.

See example code in main folder as well as class_model.py in code/ to load and use models.